/**
 * Zentrale Betreiber-Einstellungen.
 * Hier trägst du bei Bedarf Zugänge ein — leer lassen = Funktion aus.
 */
export const siteConfig = {
  /**
   * Cloudflare Web Analytics — cookieloses, DSGVO-freundliches Besucher-Dashboard
   * (kein Cookie-Banner nötig). So aktivierst du es:
   *   1. dash.cloudflare.com → "Web Analytics" → Website hinzufügen
   *      (astrofotografie-allgaeu.de).
   *   2. Den angezeigten Token kopieren und hier zwischen die Anführungszeichen
   *      setzen. Committen — fertig.
   * Leer ("") = kein Analytics, kein externer Request, nichts wird geladen.
   */
  cloudflareAnalyticsToken: '',
};
